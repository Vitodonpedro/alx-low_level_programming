#include <stdio.h>
#include <stdlid.h>
#include <time.h>
/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char x;
	char y;

	while
		(x = 'a'; x <= 'z'; x++)
		{
		putchar(x);
		}
	while
		(y = 'A'; y <= 'Z'; y++)
		{
	       putchar(y);
		}
	putchar('\n');
	return (0);
}
