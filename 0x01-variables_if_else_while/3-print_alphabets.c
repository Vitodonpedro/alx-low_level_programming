#include <stdio.h>

/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char x;
	char y;

	x = 'a';
	y = 'A';
	while
		(x <= 'z'; x++)
		{
		putchar(x);
		}
	while
		(y <= 'Z'; y++)
		{
	       putchar(y);
		}
	putchar('\n');
	return (0);
}
